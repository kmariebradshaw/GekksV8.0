    <main id="page">	     
	    <section class="background-green tac fw text-white page-header">
          <h1>{{ page.title }}</h1>
      	</section>
        <section class="index-padding">
          {{ page.content }}
	    </section>
    </main>